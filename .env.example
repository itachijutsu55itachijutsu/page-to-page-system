# A long, random, secret string used to cryptographically sign the pass
# that lets a visitor through from Page 1 to Page 2. Generate one with:
#   openssl rand -hex 32
# Keep this secret - never commit the real value.
GATE_SECRET=
